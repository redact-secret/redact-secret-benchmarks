paths-ignore:
  # Synthetic, locally-constructed credential fixtures used to benchmark
  # secret detection/redaction. None are live and none were issued by a
  # provider — see the "note" field on each fixture entry.
  - "fixtures/**"
