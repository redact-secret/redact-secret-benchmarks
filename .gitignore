node_modules/
.venv/
results-output/
*.log
.DS_Store
dist/
public/results/*.json
public/results/.*.json

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Deterministic scanner inputs are materialized locally, never committed.
/fixtures/generated/*.json
