import { categories, registry, fixtures, corpora, type Fixture } from '../catalog';
import { summarize, contentSegments } from '../model.mjs';
import { escape as e, percent, type Report } from '../types';

export const title = (label: string, description: string, eyebrow = 'BENCHMARK LAB') => `<div class="eyebrow">${e(eyebrow)}</div><h1>${e(label)}<span>.</span></h1><p class="intro">${e(description)}</p>`;
export const ranges = (items: {start: number; end: number}[]) => items.map(r => `[${r.start}, ${r.end})`).join(' · ') || 'None';
export function stats(selected: Fixture[]) {
  return `<section class="stats"><article><span>FIXTURE INSTANCES</span><strong>${selected.length}</strong><small>Each source case counted once</small></article><article><span>EXPECTED SECRET SPANS</span><strong>${selected.reduce((n,f) => n+f.expected.length,0)}</strong><small>Exact UTF-8 byte ranges</small></article><article><span>NEGATIVE CONTROLS</span><strong>${selected.filter(f => !f.expected.length).length}</strong><small>Files expected to stay clean</small></article></section>`;
}
export function comparison(selected: Fixture[], reports: Report[]) {
  const groups = summarize(selected, reports.filter(r => selected.some(f => f.category === r.category)));
  return `<section class="panel"><h2>Scanner comparison</h2><p class="intro">Latest report per source case; runs may have different timestamps. Scores use measured fixture instances only. Compare coverage before comparing scores. Versions, modes, lockfiles and matching rules are kept separate.</p>${groups.length ? `<div class="table-scroll"><table><thead><tr><th>Scanner / version</th><th>Measured files</th><th>TP / FP / FN / TN</th><th>Precision</th><th>Recall</th><th>F1</th></tr></thead><tbody>${groups.map(s => `<tr><td class="wrap-cell"><strong>${e(s.name)}</strong><small>${e(s.version ?? 'Version unavailable')} · ${e(s.mode)}</small><small>${s.sources.map((id: string) => `<a class="text-link" href="/benchmark/${e(id)}">${e(id)}</a>`).join(' · ')}</small><small>Lock: ${e(s.lockHash?.slice(0,12))} · ${e(s.matching)}</small><small>Run statuses: ${e([...new Set(s.statuses)].join(', '))}</small></td><td>${s.rows.length} / ${selected.length}</td><td>${s.rows.length ? `${s.tp} / ${s.fp} / ${s.fn} / ${s.tn}` : '—'}</td>${[s.precision,s.recall,s.f1].map(v => `<td>${percent(v)}</td>`).join('')}</tr>`).join('')}</tbody></table></div>` : '<p class="empty">No current measurements. Run <code>npm run bench</code>.</p>'}<p class="footnote">Synthetic, preliminary corpus; these results do not establish overall product quality. Cohorts may repeat token shapes. Detector pages overlap and must not be summed. No per-detector timing is measured.</p></section>`;
}
export function fixtureList(selected: Fixture[], reports: Report[]) {
  const scanners = [...new Map(reports.flatMap(r => r.scanners.map(s => [s.id, s.name] as const))).entries()];
  return `<section class="panel"><div class="section-heading"><div><h2>Fixtures</h2><p>Open a case to inspect its exact input and expected behavior.</p></div><label class="search-label">Find a fixture<input id="filter" type="search" placeholder="Name, group or source case…"></label></div><div class="table-scroll"><table><thead><tr><th>Fixture / source case</th><th>Expected</th>${scanners.map(([,name]) => `<th>${e(name)}</th>`).join('')}</tr></thead><tbody>${selected.map(f => `<tr data-search="${e(`${f.id} ${f.group} ${f.category}`.toLowerCase())}"><td><a class="text-link" href="/fixture/${e(f.slug)}">${e(f.id)} ↗</a><small>${e(f.category)} · ${e(f.group)}</small></td><td>${f.expected.length ? `${f.expected.length} secret span(s)` : 'Negative control'}</td>${scanners.map(([id]) => {
    const scanner = reports.find(r => r.category === f.category)?.scanners.find(s => s.id === id);
    const row = scanner?.status === 'complete' ? scanner.rows?.find(r => r.id === f.id) : undefined;
    return `<td>${row ? `<span class="outcome ${row.fp || row.fn ? 'miss' : 'pass'}">${row.fp || row.fn ? `${row.fp} FP / ${row.fn} missed` : 'Exact match'}</span>` : e(scanner?.status ?? 'Not measured')}</td>`;
  }).join('')}</tr>`).join('')}</tbody></table><p id="no-matches" ${selected.length ? 'hidden' : ''}>${selected.length ? 'No fixtures match this filter.' : 'No fixtures assigned yet. This detector is not covered.'}</p></div></section>`;
}
export function overview(reports: Report[]) {
  const covered = registry.detectors.filter(d => fixtures.some(f => f.detectors.includes(d.id))).length;
  return title('Benchmark overview', 'Shared synthetic inputs. Inspectable scanner results. Explore by detector or by test case.') + stats(fixtures) + comparison(fixtures,reports) + `<section class="panel"><h2>Detectors <span class="tag">${covered} / ${registry.detectors.length} WITH FIXTURES</span></h2><p class="intro">Categories follow the core detector registry. A fixture assignment describes test intent, not which scanner rule fired. Zero fixtures means coverage has not been added.</p><div class="category-grid">${registry.detectors.map(d => {
    const assigned = fixtures.filter(f => f.detectors.includes(d.id));
    const count = assigned.length, positives = assigned.filter(f => f.expected.length).length;
    return `<a class="category-card" href="/benchmark/${e(d.id)}"><strong>${e(d.title)} ↗</strong><small>${count ? `${count} fixtures · ${positives} positive / ${count-positives} negative` : 'Not covered yet'}</small></a>`;
  }).join('')}</div><p class="footnote">Registry snapshot: <a class="text-link" href="https://github.com/redact-secret/redact-secret/blob/${registry.sourceRevision}/${registry.sourcePath}">${e(registry.sourceRevision.slice(0,12))} ↗</a>. Runtime results use the installed published scanners.</p></section><section class="panel"><h2>Cases & regression suites</h2><div class="category-grid">${categories.map(c => `<a class="category-card" href="/benchmark/${e(c.id)}"><strong>${e(c.title)} ↗</strong><small>${e(c.description)}</small></a>`).join('')}</div></section>`;
}
export function fixturePage(f: Fixture, reports: Report[]) {
  const category = categories.find(c => c.id === f.category)!;
  const corpus = corpora[f.category];
  const report = reports.find(r => r.category === f.category);
  const bytes = new TextEncoder().encode(f.content);
  const highlighted = contentSegments(f.content,f.expected).map(s => s.highlighted ? `<mark>${e(s.text)}</mark>` : e(s.text)).join('');
  const issue = f.issue ?? /#(\d+)/.exec(f.group)?.[1];
  return title(f.id, f.group, 'FIXTURE / '+category.title.toUpperCase()) + `<p class="fixture-links"><a class="text-link" href="/benchmark/${e(f.category)}">← ${e(category.title)}</a>${f.detectors.map(id => `<a class="tag text-link" href="/benchmark/${e(id)}">${e(registry.detectors.find(d => d.id === id)?.title)}</a>`).join('')}${issue ? `<a class="text-link" href="https://github.com/redact-secret/redact-secret/issues/${Number(issue)}">Issue #${Number(issue)} ↗</a>` : ''}</p><div class="notice"><div><strong>${f.expected.length ? `Expect ${f.expected.length} secret span(s) to be detected` : 'Negative control: expect no findings'}</strong><p>${e(corpus.reviewStatus)}. ${e(corpus.scope ?? category.description)}</p>${corpus.milestoneReview ? `<p>${e(corpus.milestoneReview.validation)}</p>` : ''}</div></div><section class="panel"><h2>Exact synthetic input</h2><p class="intro">${e(f.path)} · ${bytes.length} UTF-8 bytes · highlighted text is the expected secret span. All values are authored test data.</p><pre class="fixture-content"><code>${highlighted || '<span class="empty">(empty file)</span>'}</code></pre><details><summary>Escaped representation (reveals whitespace, CRLF and BOM)</summary><pre class="fixture-content"><code>${e(JSON.stringify(f.content).replace(/\uFEFF/g, '\\uFEFF'))}</code></pre></details><a class="button" id="download-fixture" href="#">↓ Download exact fixture</a></section><section class="panel"><h2>Expected ranges</h2><p class="intro">UTF-8 bytes, inclusive start and exclusive end. These are not JavaScript character offsets.</p>${f.expected.length ? `<div class="table-scroll"><table><thead><tr><th>Range</th><th>Expected value</th><th>Rationale</th></tr></thead><tbody>${f.expected.map(r => `<tr><td>[${r.start}, ${r.end})</td><td class="wrap-cell"><code>${e(new TextDecoder().decode(bytes.slice(r.start,r.end)))}</code></td><td class="wrap-cell">${e(r.note)}</td></tr>`).join('')}</tbody></table></div>` : '<p>No secret spans. Any reported range is a false positive.</p>'}</section><section class="panel"><h2>Scanner outcomes</h2>${report ? `<p class="intro">Measured ${e(new Date(report.generatedAt).toLocaleString())} · <a class="text-link" href="/results/${e(f.category)}.json" download>Source report ↗</a></p><div class="table-scroll"><table><thead><tr><th>Scanner</th><th>Outcome</th><th>Reported byte ranges</th></tr></thead><tbody>${report.scanners.map(s => {
    const row = s.status === 'complete' ? s.rows?.find(r => r.id === f.id) : undefined;
    return `<tr><td>${e(s.name)}<small>${e(s.version)} · ${e(s.mode)}</small></td><td>${row ? `${row.tp} TP / ${row.fp} FP / ${row.fn} FN / ${row.tn} TN` : e(s.status)}<small>${e(s.message ?? '')}</small></td><td>${row ? e(ranges(row.actual)) : '—'}</td></tr>`;
  }).join('')}</tbody></table></div>` : '<p>No current report for these fixture bytes. Run <code>npm run bench</code>.</p>'}</section>`;
}
