import { figure, statusMark, OUTCOME_NAME, type Outcome } from '../components';

const OUTCOME_TEXT: Record<Outcome, [string, string]> = {
  EXACT: ['A solid bar the same length as the secret', 'Protected'],
  COVERED: ['A longer solid bar that stays inside the envelope', 'Protected'],
  OVERBROAD: ['A solid bar that runs past the envelope', 'Protected, and counted as over-redaction'],
  PARTIAL: ['A hatched bar over part of the secret', 'Exposed'],
  MISS: ['A dashed empty frame where the secret is', 'Exposed'],
};

/** Protocol, glossary and reproduction, once, so no other page has to carry a disclaimer. */
export function howToRead(): string {
  const example = figure({ question: 'Does it miss real secrets?', value: { point: 0, bound: 0.026894, n: 139, direction: 'upper' }, observed: { count: 0, of: 139, noun: 'secret spans leaked' }, definition: 'Leaked span rate. Lower is better. 95% pessimistic bound.' });
  return `<div class="page-head"><div><p class="eyebrow">PROTOCOL V4 · ACCOUNTING V1.1</p><h1>How to read this site</h1><div class="meta"><span>Same inputs. Three questions. One run id.</span></div></div></div>
  <div class="cols2"><section class="prose"><h2 class="h2-compact">One number, four pieces of information</h2><p>Every figure has the same shape. The <b class="strong">direction word</b> says which way the bound points: <i>at most</i> for leaks and false alarms, <i>at least</i> for twin discrimination. The <b class="strong">large number</b> is the published pessimistic bound, not the observed rate. The <b class="strong">bold fraction</b> is what was observed and out of how many. The last line is the definition.</p><p>On the interval, the triangle is the observed value and the vertical tick is the published bound. Only the pessimistic side is published, so only that side is drawn.</p><p>${statusMark('withheld', 'Few samples')} means n is small enough that the bound stays wide. ${statusMark('withheld', 'Withheld')} means no rate is published at all: the group is below the minimum sample size, too much of it is still pending review, or too few secrets have an authored near-twin. ${statusMark('not-measured')} is not a result: a scanner did not run, and its rows are never read as zero.</p></section><div>${example}</div></div>
  <section class="section"><h2 class="h2-compact">Redaction bars</h2><p class="small">On a fixture page, green marks the bytes that must be redacted, an ink underline marks the envelope, and each scanner's bar shows what it covered. The five outcomes differ by shape, so they read without colour.</p><div class="tbl"><table><thead><tr><th scope="col">Outcome</th><th scope="col">Shown as</th><th scope="col">Bar</th><th scope="col">Counts as</th></tr></thead><tbody>${(Object.keys(OUTCOME_TEXT) as Outcome[]).map(o => `<tr><td><code>${o}</code></td><td>${OUTCOME_NAME[o]}</td><td>${OUTCOME_TEXT[o][0]}</td><td>${OUTCOME_TEXT[o][1]}</td></tr>`).join('')}</tbody></table></div></section>
  <section class="section prose"><h2 class="h2-compact">Three axes, not one cohort</h2><p>Every fixture declares a <b class="strong">kind</b> (must-redact, must-not-flag, policy), an evidence <b class="strong">tier</b> (T1 provider-documented, T2 tool-corroborated, T3 project policy, T0 pending) and a <b class="strong">role</b> per span. Kind decides which metric a file feeds; tier decides which rows may share a denominator. They never constrain each other, so a provider-documented public identifier can be a T1 negative control. Classification is authored from input construction and provider evidence, never from scanner output, and unknown input fails closed into T0.</p>
  <h2 class="h2-compact">Inner span and envelope</h2><p>Each secret span may carry an authored envelope: the widest byte range a finding may extend to at no cost, with a reason. Connection strings declare the password as the span and the URI as the envelope; OTP URIs, Bearer headers and quoted generic assignments do the same. An envelope is never widened in response to a scanner, is covered by the corpus hash and is listed on every fixture page. The default envelope is the span itself.</p>
  <h2 class="h2-compact">Per-span outcome lattice</h2><p>Against the deduplicated findings on a file, each secret span is <code>EXACT</code> (a finding equals it), <code>COVERED</code> (a single finding contains it and stays inside the envelope), <code>OVERBROAD</code> (a single finding contains it and exceeds the envelope), <code>PARTIAL</code> (findings overlap it but none contains it) or <code>MISS</code>. Coverage is per finding, not per union: a secret straddled by two findings is <code>PARTIAL</code> and counts as leaked, because two redactions do not jointly guarantee one covered secret. Nothing improves by reporting less, and nothing improves by reporting more.</p>
  <h2 class="h2-compact">Headline numbers, per kind × tier</h2><p><b class="strong">Leaked span rate</b> = spans with outcome PARTIAL or MISS ÷ secret spans (macro over spans, so a long PEM key does not outweigh forty tokens); secondary leaked byte rate = uncovered secret bytes ÷ secret bytes. <b class="strong">False alarm rate</b> = control files with at least one finding ÷ control files. <b class="strong">Collateral ratio</b> = redacted bytes outside every envelope ÷ secret bytes, so whole-file reporting becomes a large number instead of a footnote. <b class="strong">Twin discrimination</b> = pairs where the positive is covered and its one-mutation twin stays clean ÷ authored pairs; un-twinned positives are shown as a coverage gap, never averaged in. Exact-range agreement survives only as a diagnostic marked non-comparable. Precision, recall and F1 are not exported; a report containing them is rejected.</p>
  <h2 class="h2-compact">Accounting v1.1: the published figure is the worst defensible bound</h2><p>Anything unmeasured, unstable or unreviewed consumes denominator rather than disappearing from it. Every rate is shown as its Wilson bound on the pessimistic side with the point estimate and <code>n</code> beside it; the collateral ratio is unbounded above and carries no interval by decision. A group prints <code>insufficient-evidence</code> instead of a rate when its denominator is below the suite minimum or when too much of its candidate surface is still pending (T0), and <code>insufficient-coverage</code> instead of a twin rate when too few of its secrets have an authored fake. An overbroad finding does not leak the secret, but it is not a discriminated twin. Each scanner is replayed over the same files; a disagreement is <code>unstable</code> and its findings are discarded. Floors are suite configuration in <code>qualification/suite-v1.json</code>.</p><p>Bounds are computed once, when the benchmark runs, and published: per suite in each suite report, and across suites and per detector in <code>summary.json</code>. This site displays those values and derives none. Before reading a suite report it re-verifies every row and group total against the fixture bytes, and it checks that the summary's counts add up to its suites.</p>
  <h2 class="h2-compact">One run, one id; baselines instead of tables</h2><p><code>npm run bench</code> stamps one run id into every suite report and writes <code>run.json</code>. Cross-suite views read only reports sharing the newest run id and name any stale suite instead of silently mixing moments. Released comparison points are stored as (fixture, scanner) → outcome in <code>baselines/</code>; the Workbench lists rows that changed since the newest baseline.</p>
  <h2 class="h2-compact">Same files, explicit output contracts</h2><p>Every scanner receives the same temporary directory. TruffleHog verification and update checks are disabled, and verified, unknown and unverified output are included. Failed or unavailable scanners are not scored. Adapters map AWS RawV2 components, Shopify token-plus-domain output and TruffleHog’s canonical Postgres URI back to source bytes using scanner output alone; they never consult expected ranges, and ambiguous transformations fail the run.</p>
  <h2 class="h2-compact">What this does not claim</h2><ul><li>Not a representative sample of production credentials; not a product ranking. Other scanners are reference values on the same inputs.</li><li>Leaked span rate is corpus-relative. It is not accuracy, and cross-tool comparison is valid only within one (kind × tier) group on one run id.</li><li>T1 means a documented lexical or structural shape, not issuance, checksum validity or liveness. No live verification.</li><li>Envelopes and policy expectations are this project’s redaction policy. A disagreement there is a policy difference, not a defect.</li><li>Twin discrimination is bounded by which twins have been authored; AWS ID/secret pairs have no single-mutation twin yet.</li><li>Duration is diagnostic, not a speed benchmark. Ranges and counts are exported; matched values and raw scanner output never are.</li></ul>
  <h2 class="h2-compact">Reproduce</h2><pre class="cmd"><code>npm ci
npm run fixtures:check
npm test
npm run test:integration
npm run bench -- --strict
npm run baseline -- --save &lt;version&gt;   # after a complete run
npm run baseline:report
npm run dev</code></pre><p class="small">Legacy schema-v1/v2/v3 reports are rejected and must be regenerated. The full rationale is in <code>docs/measurement-v4.md</code> and the decision records in <code>docs/decisions/</code>.</p></section>`;
}
