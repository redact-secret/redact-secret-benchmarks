export function methodology() {
  return `<div class="eyebrow">THE PROTOCOL</div><h1>How we measure.</h1><p class="intro">Same inputs. Separate questions. Inspectable evidence.</p>
  <section class="panel prose"><h2>Four purposes, no combined score</h2><p>Reviewed credential formats use pinned upstream source contracts, required companion context and parseable cryptographic controls. “Reviewed” means lexical/structural evidence, not provider issuance, checksum validation or live credentials. Support by all scanners is not a condition of inclusion: unsupported formats and misses stay visible.</p><p>Standalone values & masking policy measure isolated IDs, generic literals, bearer values, OTP seeds and password-only URI ranges. Malformed, examples & controls preserve historical expectations for malformed tokens, public examples, placeholders, references and benign text. These are project policy tests, not universal provider detection ground truth. Format review pending retains observations without TP/FP/FN or rates.</p><p>Every fixture has a cohort, rationale and, for reviewed formats, source links. Missing metadata blocks a run. No combined precision, recall or F1 is exported or displayed across cohorts. Positive-only format samples do not establish balanced precision or F1. Context repetitions are not independent format coverage.</p><h2>Same files, explicit output contracts</h2><p>Every scanner receives the same temporary directory. TruffleHog verification and update checks are disabled and verified, unknown and unverified output are included. Failed or unavailable scanners are not scored. Provider detection can require companion fields: AWS pairs include the ID and secret; Shopify includes a shop domain. Bare components remain separate masking tests.</p><p>The adapter maps AWS RawV2 ID/secret components and the token from Shopify’s token-plus-domain Raw output using the scanner output alone. Companion domains are not secrets. Postgres whole-URI reports retain their whole-URI scope. Adapters never consult expected ranges to repair output. Unknown or ambiguous transformations fail the scanner run.</p><h2>Exact ranges and containment are different observations</h2><p>Ranges are UTF-8 byte offsets [start, end). Identical reported ranges are deduplicated. An exact match requires the same path and range. A partial overlap leaves an unmatched expected range and an extra reported range. Historical TP/FP/FN fields in cohort JSON refer only to that cohort’s authored policy.</p><p>Containment counts an expected span when a reported range fully encloses it. Broader only means contained without an exact match. Reporting an entire file can satisfy containment, so it is not precision or safe redaction. Negative controls count clean files separately. Pending-review rows have no scores.</p><h2>Evidence and limits</h2><p>All 25 detector families have an audit entry in the overview. A related provider detector is not proof of variant support. Unreviewed variants are explicit coverage gaps. Existing fixture contents and expectations are retained; newly reviewed controls are added separately. Corpus hashes include classifications and evidence. Schema-v1/v2 mixed reports are rejected and must be regenerated.</p><p>This project-maintained synthetic corpus is not a neutral sample of production credentials. Source review does not replace independent human review; draft status remains visible. No product ranking, live validation, throughput, PyPI/CLI parity or streaming claim is made.</p><h2>Reproduce</h2><pre>npm ci
npm run fixtures:check
npm test
npm run test:integration
npm run bench -- --strict
npm run dev</pre><p>Use <code>--category=common-formats</code> for the new source-backed control suite. All cohorts remain separated on overview, detector and suite pages and in exported JSON.</p></section>`;
}
