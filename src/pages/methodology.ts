export function methodology() {
  return `<div class="eyebrow">THE PROTOCOL · V4</div><h1>How we measure.</h1><p class="intro">Same inputs. Three questions. One run id. Every caveat lives here, once.</p>
  <section class="panel prose"><h2>Three axes, not one cohort</h2><p>Every fixture declares a <strong>kind</strong> (must-redact, must-not-flag, policy), an evidence <strong>tier</strong> (T1 provider-documented, T2 tool-corroborated, T3 project policy, T0 pending) and a <strong>role</strong> per span. Kind decides which metric a file feeds; tier decides which rows may share a denominator. They never constrain each other, so a provider-documented public identifier can be a T1 negative control. Classification is authored from input construction and provider evidence, never from scanner output, and unknown input fails closed into T0.</p>
  <h2>Inner span and envelope</h2><p>Each secret span may carry an authored envelope: the widest byte range a finding may extend to at no cost, with a reason. Connection strings declare the password as the span and the URI as the envelope; OTP URIs, Bearer headers and quoted generic assignments do the same. An envelope is never widened in response to a scanner, is covered by the corpus hash and is listed on every fixture page. Default envelope is the span itself.</p>
  <h2>Per-span outcome lattice</h2><p>Against the deduplicated findings on a file, each secret span is <code>EXACT</code> (a finding equals it), <code>COVERED</code> (a single finding contains it and stays inside the envelope), <code>OVERBROAD</code> (a single finding contains it and exceeds the envelope), <code>PARTIAL</code> (findings overlap it but none contains it) or <code>MISS</code>. Coverage is per finding, not per union: a secret straddled by two findings is <code>PARTIAL</code> and counts as leaked, because two redactions do not jointly guarantee one covered secret. Nothing improves by reporting less, and nothing improves by reporting more.</p>
  <h2>Headline numbers, per kind × tier</h2><p><strong>Leaked span rate</strong> = spans with outcome PARTIAL or MISS ÷ secret spans (macro over spans, so a long PEM key does not outweigh forty tokens); secondary leaked byte rate = uncovered secret bytes ÷ secret bytes. <strong>False alarm rate</strong> = control files with at least one finding ÷ control files; secondary mean findings per flagged control. <strong>Collateral ratio</strong> = redacted bytes outside every envelope ÷ secret bytes, so whole-file reporting becomes a large number instead of a footnote. <strong>Twin discrimination</strong> = pairs where the positive is covered and its one-mutation twin stays clean ÷ authored pairs; un-twinned positives are shown as a coverage gap, never averaged in. Exact-range agreement survives only as a diagnostic marked non-comparable. Precision, recall and F1 are not exported; a report containing them is rejected.</p>
  <h2>Accounting v1.1: the published figure is the worst defensible bound</h2><p>Anything unmeasured, unstable or unreviewed consumes denominator rather than disappearing from it. Every rate is shown as its Wilson bound on the pessimistic side (<code>≤</code> for leak, false-alarm and collateral direction, <code>≥</code> for twin, coverage and measurable share) with the point estimate and <code>n</code> beside it; the collateral ratio is unbounded above and carries no interval by decision. A group prints <code>insufficient-evidence</code> instead of a rate when its denominator is below the suite minimum or when too much of its candidate surface is still pending (T0), and <code>insufficient-coverage</code> instead of a twin rate when too few of its secrets have an authored fake. An overbroad finding does not leak the secret, but it is not a discriminated twin. Each scanner is replayed over the same files; a disagreement is <code>unstable</code> and its findings are discarded. Floors are suite configuration in <code>qualification/suite-v1.json</code>; every group and its v1.0 → v1.1 delta is recomputed in this browser from the report rows.</p>
  <h2>One run, one id; baselines instead of tables</h2><p><code>npm run bench</code> stamps one run id into every suite report and writes <code>run.json</code>. Cross-suite views aggregate only reports sharing the newest run id and name any stale suite instead of silently mixing moments. Released comparison points are stored as (fixture, scanner) → outcome in <code>baselines/</code>; the dashboard marks rows that changed since the newest baseline, and the release comparison document is generated from those files.</p>
  <h2>Same files, explicit output contracts</h2><p>Every scanner receives the same temporary directory. TruffleHog verification and update checks are disabled and verified, unknown and unverified output are included. Failed or unavailable scanners are not scored. Adapters map AWS RawV2 components, Shopify token-plus-domain output and TruffleHog’s canonical Postgres URI back to source bytes using scanner output alone; they never consult expected ranges, and ambiguous transformations fail the run. Envelopes make such repair unnecessary: a wider-but-acceptable finding is simply covered.</p>
  <h2>What v4 still does not claim</h2><ul><li>Not a representative sample of production credentials; not a product ranking.</li><li>Leaked span rate is corpus-relative. It is not accuracy, and cross-tool comparison is valid only within one (kind × tier) group on one run id. T2 groups are compared within T2 with the tier visible; they exist partly because tools disagree.</li><li>T1 means a documented lexical or structural shape, not issuance, checksum validity or liveness. No live verification.</li><li>Envelopes and policy expectations are this project’s redaction policy. A disagreement there is a policy difference, not a defect.</li><li>Twin discrimination is bounded by which twins have been authored; AWS ID/secret pairs have no single-mutation twin yet.</li><li>Duration is diagnostic, not a speed benchmark. Ranges and counts are exported; matched values and raw scanner output never are.</li></ul>
  <h2>Reproduce</h2><pre>npm ci
npm run fixtures:check
npm test
npm run test:integration
npm run bench -- --strict
npm run baseline -- --save &lt;version&gt;   # after a complete run
npm run baseline:report
npm run dev</pre><p>Legacy schema-v1/v2/v3 reports are rejected and must be regenerated. The full rationale, migration phases and open questions are in <code>docs/measurement-v4.md</code> and the decision record in <code>docs/decisions/</code>.</p></section>`;
}
