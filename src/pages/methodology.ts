export function methodology() {
  return `<div class="eyebrow">THE PROTOCOL</div><h1>How we measure.</h1><p class="intro">One corpus. Explicit ground truth. Inspectable scoring.</p><section class="panel prose"><h2>Exact ranges, shared files</h2><p>Every scanner receives the same files in a temporary directory. Ground truth uses UTF-8 byte offsets with an inclusive start and exclusive end. A true positive requires the exact path and range. A partial overlap counts as one false positive and one unmatched expected range. Identical reported ranges are deduplicated, regardless of detector.</p><h2>Containment observation</h2><p>Separately, an expected secret is contained when any reported range fully encloses it. Each expected span counts once, even with duplicate or overlapping findings; a single finding can contain several expected secrets. Partial overlap does not count. Broader only counts contained secrets without an exact match. Reporting an entire file can satisfy containment, so this is not precision, safe redaction, or overall accuracy. TruffleHog database URLs retain their reported whole-URL scope; adapters never trim them using ground truth.</p><h2>Metrics</h2><p>Precision = TP / (TP + FP). Recall = TP / (TP + FN). F1 = 2TP / (2TP + FP + FN). A zero denominator is shown as an em dash. True negatives count negative files with no findings; they are not mixed with secret-span metrics.</p><h2>Scope and limitations</h2><p>These project-maintained synthetic corpora include regressions and fixtures generated from Redact Secret’s own detector list. They are not a neutral sample of secret formats or production traffic, and draft review status is retained. Synthetic tokens may be filtered deliberately by scanners. These results validate the workflow and must not be used to rank products. Missing and failed tools receive no score. TruffleHog verification and update checks are disabled; detection runs locally. No PyPI or CLI parity claims are made for the npm adapter.</p><h2>Reproduce a run</h2><pre>npm ci\nnpm run bench\nnpm run dev</pre><p>Use <code>npm run bench -- --strict</code> to fail if any scanner is unavailable. Use <code>--category=accuracy</code> to update one category. Results refresh automatically in this browser. Run the benchmark before building a static snapshot.</p><h2>Add a category</h2><p>Register a category in <code>benchmarks/categories.json</code>. Categories using the accuracy protocol need only a new corpus. A new measurement protocol adds a handler in <code>benchmarks/run.mjs</code> and a renderer in <code>src/pages/</code>, registered in the frontend. Case navigation comes from the corpus registry; detector navigation comes from <code>benchmarks/detectors.json</code>. Assign every fixture in <code>benchmarks/fixture-detectors.json</code>; an empty list means a shared case without a detector assignment. Open <code>/benchmark</code> for the overview and <code>/fixture/&lt;category&gt;--&lt;id&gt;</code> for exact synthetic inputs.</p></section>`;
}
